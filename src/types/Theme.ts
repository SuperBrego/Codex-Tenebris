// theme/theme.ts
export type Theme = {
  name: string;
  colors: {
    background: string;
    surface: string;
    border: string;
    primary: string;
    primaryText: string;
    text: string;
    accent: string;
    danger: string;
    highlight: string;
  };
};

